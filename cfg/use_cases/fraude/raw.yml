layer: raw
extends: "../../finance/raw/transactions_http.yml"
io:
  source:
    params:
      risk_profile: fraud
transform:
  sql:
    - |
      SELECT *
      FROM __BASE__
      WHERE risk_flag = true OR score >= 70
dq:
  expectations:
    - type: condition
      expr: "score <= 100"
